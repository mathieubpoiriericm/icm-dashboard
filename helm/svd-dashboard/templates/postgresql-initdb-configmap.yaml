apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "svd-dashboard.fullname" . }}-postgresql-initdb
  labels:
    {{- include "svd-dashboard.labels" . | nindent 4 }}
data:
  01-setup.sql: |
{{ .Files.Get "sql/setup.sql" | indent 4 }}
  02-external-tables.sql: |
{{ .Files.Get "sql/add_external_data_tables.sql" | indent 4 }}
