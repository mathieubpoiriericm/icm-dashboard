{{/*
  Scoped Secrets — each workload only receives the credentials it needs.
*/}}

# --- Database credentials (PostgreSQL, Pipeline, trigger_update.R) ---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "svd-dashboard.fullname" . }}-db-credentials
  labels:
    {{- include "svd-dashboard.labels" . | nindent 4 }}
type: Opaque
stringData:
  DB_HOST: {{ include "svd-dashboard.fullname" . }}-postgresql
  DB_PORT: "5432"
  DB_NAME: {{ .Values.postgresql.credentials.database | quote }}
  DB_USER: {{ .Values.postgresql.credentials.username | quote }}
  DB_PASSWORD: {{ .Values.postgresql.credentials.password | quote }}
  POSTGRES_DB: {{ .Values.postgresql.credentials.database | quote }}
  POSTGRES_USER: {{ .Values.postgresql.credentials.username | quote }}
  POSTGRES_PASSWORD: {{ .Values.postgresql.credentials.password | quote }}
---
# --- Pipeline-only secrets (API keys, notification URLs) ---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "svd-dashboard.fullname" . }}-pipeline-secrets
  labels:
    {{- include "svd-dashboard.labels" . | nindent 4 }}
type: Opaque
stringData:
  ANTHROPIC_API_KEY: {{ .Values.secrets.anthropicApiKey | quote }}
  NCBI_API_KEY: {{ .Values.secrets.ncbiApiKey | quote }}
  ENTREZ_EMAIL: {{ .Values.secrets.entrezEmail | quote }}
  UNPAYWALL_EMAIL: {{ .Values.secrets.unpaywallEmail | quote }}
  PIPELINE_NOTIFY_URLS: {{ .Values.secrets.notifyUrls | quote }}
  PIPELINE_HEALTHCHECK_URL: {{ .Values.secrets.healthcheckUrl | quote }}
---
# --- .Renviron file for dashboard (DB creds + NCBI vars for trigger_update.R) ---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "svd-dashboard.fullname" . }}-renviron
  labels:
    {{- include "svd-dashboard.labels" . | nindent 4 }}
type: Opaque
stringData:
  .Renviron: |
    DB_HOST={{ include "svd-dashboard.fullname" . }}-postgresql
    DB_PORT=5432
    DB_NAME={{ .Values.postgresql.credentials.database }}
    DB_USER={{ .Values.postgresql.credentials.username }}
    DB_PASSWORD={{ .Values.postgresql.credentials.password }}
    NCBI_API_KEY={{ .Values.secrets.ncbiApiKey }}
    ENTREZ_EMAIL={{ .Values.secrets.entrezEmail }}
---
{{- if .Values.notifications.healthchecks.enabled }}
# --- Healthchecks Django SECRET_KEY ---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "svd-dashboard.fullname" . }}-healthchecks
  labels:
    {{- include "svd-dashboard.labels" . | nindent 4 }}
type: Opaque
stringData:
  SECRET_KEY: {{ required "notifications.healthchecks.secretKey is required when healthchecks is enabled" .Values.notifications.healthchecks.secretKey | quote }}
{{- end }}
