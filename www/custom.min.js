function progressiveRetry(fn,delays){fn();var finalDelay=(delays&&delays.length>0)?delays[delays.length-1]:200;setTimeout(fn,finalDelay);}Shiny.addCustomMessageHandler('rerunPythonPlotSizing',function(msg){var iframe=document.querySelector('iframe[src="python_plot.html"]');if (!iframe||!iframe.contentWindow) return;var win=iframe.contentWindow;if (typeof win.adjustLabelBackgrounds==='function') win.adjustLabelBackgrounds();if (typeof win.adjustLegendBox==='function') win.adjustLegendBox();if (typeof win.adjustLegendBoxMOA==='function') win.adjustLegendBoxMOA();if (typeof win.adjustDrugLabelCollisions==='function') win.adjustDrugLabelCollisions();if (typeof win.avoidCognitiveOverlap==='function') win.avoidCognitiveOverlap();});window.tippyTimeout=null;window.tippyInstances=[];window.initializeTippy=function(){if (window.tippyTimeout){clearTimeout(window.tippyTimeout);}window.tippyTimeout=setTimeout(function(){var elements=document.querySelectorAll('[data-tippy-content]');if (elements.length===0) return;var elementsToInit=[];elements.forEach(function(el){if (!el._tippy){elementsToInit.push(el);}});if (elementsToInit.length>0){var newInstances=tippy(elementsToInit,{allowHTML:true,theme:'custom',placement:'top',arrow:true,interactive:false,appendTo:document.body,maxWidth:'none',trigger:'mouseenter focus',onShow:function(instance){var maxWidth=instance.reference.getAttribute('data-tippy-maxWidth');if (maxWidth){var content=instance.popper.querySelector('.tippy-content');if (content){content.style.maxWidth=maxWidth;content.style.whiteSpace='normal';content.style.wordWrap='break-word';}}}});window.tippyInstances=window.tippyInstances.concat(newInstances);}},100);};$(document).on('init.dt draw.dt',function(e,settings){window.initializeTippy();});$(document).on('shiny:connected',function(){var retryCount=0;var maxRetries=50;var checkSlider=setInterval(function(){retryCount++;var sliderInput=$('#sample_size_filter');if (sliderInput.length>0&&sliderInput.data('ionRangeSlider')){clearInterval(checkSlider);var slider=sliderInput.data('ionRangeSlider');var sliderContainer=sliderInput.closest('.irs');slider.update({grid:false,prettify:function(num){return num.toLocaleString();}});var min=15;var max=3156;var range=max-min;var tickValues=[15,500,1000,1500,2000,2500,3000,3156];var customGrid=$('<div class="irs-grid custom-grid" style="display:block;"></div>');tickValues.forEach(function(val){var percent=((val-min)/range)*100;var tick=$('<span class="irs-grid-pol"></span>');tick.css('left',percent+'%');customGrid.append(tick);var label=$('<span class="irs-grid-text"></span>');label.css('left',percent+'%');label.text(val.toLocaleString());customGrid.append(label);});sliderContainer.append(customGrid);}else if (retryCount>=maxRetries){clearInterval(checkSlider);}},100);});$(document).on('init.dt',function(e,settings){var wrapper=$(e.target).closest('.dataTables_wrapper');var scrollBody=wrapper.find('.dataTables_scrollBody');var scrollContainer=wrapper.find('.dataTables_scroll');wrapper.find('.top-scrollbar').remove();if (scrollBody.length>0&&scrollContainer.length>0){var scrollBodyEl=scrollBody[0];var topScrollbar=$('<div class="top-scrollbar"></div>');var topScrollbarInner=$('<div class="top-scrollbar-inner"></div>');topScrollbar.append(topScrollbarInner);function updateTopScrollbarWidth(){var bodyClientWidth=scrollBodyEl.clientWidth;topScrollbar.css('width',bodyClientWidth+'px');var scrollWidth=scrollBodyEl.scrollWidth;topScrollbarInner.css('width',scrollWidth+'px');}wrapper.data('updateTopScrollbarWidth',updateTopScrollbarWidth);progressiveRetry(updateTopScrollbarWidth,[100,300,1000]);$(window).on('resize.topScrollbar',updateTopScrollbarWidth);scrollContainer.before(topScrollbar);var topScrollbarEl=topScrollbar[0];var syncRAF=null;var syncSource=null;var lastSyncTime=0;var SYNC_COOLDOWN=16;function syncScroll(source){var now=performance.now();if (syncSource&&syncSource!==source&&(now-lastSyncTime)<SYNC_COOLDOWN){return;}if (syncRAF){cancelAnimationFrame(syncRAF);}syncSource=source;syncRAF=requestAnimationFrame(function(){lastSyncTime=performance.now();if (source==='top'){scrollBodyEl.scrollLeft=topScrollbarEl.scrollLeft;}else{topScrollbarEl.scrollLeft=scrollBodyEl.scrollLeft;}syncRAF=null;setTimeout(function(){syncSource=null;},SYNC_COOLDOWN);});}topScrollbarEl.addEventListener('scroll',function(){syncScroll('top');},{passive:true});scrollBodyEl.addEventListener('scroll',function(){syncScroll('body');},{passive:true});var scrollEndTimer=null;var scrollStateRAF=null;var isScrolling=false;function onScrollStart(){if (scrollStateRAF) return;scrollStateRAF=requestAnimationFrame(function(){scrollStateRAF=null;if (!isScrolling){isScrolling=true;scrollBodyEl.classList.add('is-scrolling');}clearTimeout(scrollEndTimer);scrollEndTimer=setTimeout(function(){isScrolling=false;scrollBodyEl.classList.remove('is-scrolling');},150);});}scrollBodyEl.addEventListener('scroll',onScrollStart,{passive:true});topScrollbarEl.addEventListener('scroll',onScrollStart,{passive:true});}});$(document).on('draw.dt',function(e,settings){var wrapper=$(e.target).closest('.dataTables_wrapper');var updateFn=wrapper.data('updateTopScrollbarWidth');if (typeof updateFn==='function'){progressiveRetry(updateFn,[50,200]);}});function connectBslibControlsToTable(tableId,pageLengthInputId,searchInputId){var currentTable=null;var searchTimeout=null;function connectControls(table){if (!table) return;if (currentTable===table) return;currentTable=table;$('#'+pageLengthInputId).off('change.dtBslib').on('change.dtBslib',function(){var newLength=parseInt($(this).val(),10);if (!isNaN(newLength)&&currentTable){currentTable.page.len(newLength).draw();}});$('#'+searchInputId).off('input.dtBslib').on('input.dtBslib',function(){var searchVal=$(this).val();clearTimeout(searchTimeout);searchTimeout=setTimeout(function(){if (currentTable){currentTable.search(searchVal).draw();}},300);});}var existingTable=$('#'+tableId+' table.dataTable');if (existingTable.length>0&&$.fn.DataTable.isDataTable(existingTable)){connectControls(existingTable.DataTable());}$(document).on('init.dt',function(e,settings){var tableContainer=$(e.target).closest('#'+tableId);if (tableContainer.length>0){connectControls($(e.target).DataTable());}});}$(document).on('shiny:connected',function(){connectBslibControlsToTable('firstTable','table1_page_length','table1_search');connectBslibControlsToTable('secondTable','table2_page_length','table2_search');});function syncControlsWidth(tableId){var tableContainer=$('#'+tableId);var tableWrapper=tableContainer.find('.dataTables_wrapper');var scrollContainer=tableWrapper.find('.dataTables_scroll');var controlsContainer=tableContainer.prev('.dt-bslib-controls');if (scrollContainer.length>0&&controlsContainer.length>0){var scrollWidth=scrollContainer.outerWidth();controlsContainer.css('width',scrollWidth+'px');}}$(document).on('init.dt draw.dt',function(e,settings){var tableId=settings.sTableId;progressiveRetry(function(){syncControlsWidth(tableId);},[50,200]);});$(window).on('resize',function(){syncControlsWidth('firstTable');syncControlsWidth('secondTable');});function fixOrphanDtLabels(){$('.dt-bslib-label').each(function(){var $label=$(this);if ($label.is('label')){var $span=$('<span>').addClass('dt-bslib-label').text($label.text());$label.replaceWith($span);}});}$(document).on('shiny:connected',fixOrphanDtLabels);$(document).on('init.dt',function(){setTimeout(fixOrphanDtLabels,100);});
